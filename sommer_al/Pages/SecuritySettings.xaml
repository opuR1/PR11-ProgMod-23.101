<Page x:Class="sommer_al.Pages.SecuritySettings"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:sommer_al.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="1000"
      Title="SecuritySettings" Background="White">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260" />
            <ColumnDefinition />
            <ColumnDefinition Width="260" />
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center" Width="400">
            <TextBlock Text="Настройки безопасности" FontSize="24" FontWeight="Bold" 
                       Margin="0,0,0,30" HorizontalAlignment="Center"/>

            <Border BorderBrush="#4CAF50" BorderThickness="2" CornerRadius="8" 
                    Padding="20" Background="#E8F5E9">
                <StackPanel>
                    <TextBlock Text="Двухфакторная аутентификация" FontWeight="Bold" 
                               FontSize="18" Margin="0,0,0,10" Foreground="#2E7D32"/>

                    <CheckBox x:Name="cbEnableTwoFactor" Content="Включить двухфакторную аутентификацию"
                              FontSize="14" Margin="0,0,0,15"/>

                    <TextBlock Text="При включенной двухфакторной аутентификации при каждом входе будет требоваться код подтверждения, отправляемый на ваш email."
                               TextWrapping="Wrap" Margin="0,0,0,20" FontSize="13"/>

                    <TextBlock x:Name="tbCurrentEmail" FontWeight="Bold" Margin="0,0,0,5"/>
                    <TextBlock x:Name="tbStatus" FontWeight="Bold" Margin="0,0,0,20"/>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <Button x:Name="btnSave" Content="Сохранить настройки" 
                                Click="btnSave_Click" Height="35" Width="150"
                                Background="#4CAF50" Foreground="White" Margin="0,0,10,0"/>
                        <Button x:Name="btnCancel" Content="Назад" 
                                Click="btnCancel_Click" Height="35" Width="100"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</Page>