<Page x:Class="sommer_al.Pages.ProductEdit"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:sommer_al.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="1000"
      Title="Редактирование товара" Background="White">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto" Margin="20">
            <StackPanel>
                <TextBlock x:Name="tblMode" Text="Добавление/Редактирование товара" FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>

                <TextBlock Text="Название товара: *" Margin="0,5,0,0"/>
                <TextBox x:Name="tbName" Height="30"/>

                <TextBlock Text="Артикул: *" Margin="0,5,0,0"/>
                <TextBox x:Name="tbArticle" Height="30"/>

                <TextBlock Text="Сезон: *" Margin="0,5,0,0"/>
                <ComboBox x:Name="cmbSeason" Height="30" DisplayMemberPath="Value" SelectedValuePath="Key"/>

                <TextBlock Text="Цена (Продажа): *" Margin="0,5,0,0"/>
                <TextBox x:Name="tbPrice" Height="30" InputScope="Number"/>

                <TextBlock Text="Себестоимость: *" Margin="0,5,0,0"/>
                <TextBox x:Name="tbCostPrice" Height="30" InputScope="Number"/>

                <TextBlock Text="Комментарий/Описание:" Margin="0,5,0,0"/>
                <TextBox x:Name="tbDescription" Height="100" TextWrapping="Wrap" AcceptsReturn="True"/>

                <TextBlock x:Name="tblError" Foreground="Red" TextWrapping="Wrap" Margin="0,15,0,0" Height="40"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                    <Button x:Name="btnSave" Content="Сохранить" Click="BtnSave_Click" Width="120" Margin="5"/>
                    <Button x:Name="btnCancel" Content="Отмена" Click="BtnCancel_Click" Width="120" Margin="5"/>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>

        <Border Grid.Column="1" BorderBrush="LightGray" BorderThickness="1" Margin="20" Width="200" Height="200" VerticalAlignment="Top">
            <TextBlock Text="Место для фото товара" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="Gray"/>
        </Border>
    </Grid>
</Page>
